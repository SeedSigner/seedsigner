repos:
  - repo: local
    hooks:
      - id: run_extract_messages
        name: Update and check messages.pot
        language: system
        entry: python3
        args: ["l10n/run_extract_messages.py"]
        files: ^src/
        require_serial: true
        stages: [pre-commit]
